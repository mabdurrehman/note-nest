generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Note {
  id         Int      @id @default(autoincrement())
  content    String
  created_at DateTime @default(now())
}